## Python environment

Conda environment `genai_project`. Always use the full interpreter path - never plain `python`, never rely on implicit activation.

`/opt/miniconda3/envs/genai_projectbin/python`

Common commands:

```bash
/opt/miniconda3/envs/genai_project/bin/python script.py
/opt/miniconda3/envs/genai_project/bin/python -m <module>
/opt/miniconda3/envs/genai_project/bin/python -m pytest
/opt/miniconda3/envs/genai_project/bin/python -m pip install -r requirements.txt
```

## Plans

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any.

## Workflow folders

- `.claude/sketches/` — Pre-plan context documents generated by `/grill-me`. Read the latest sketch before entering Plan Mode.
- `.claude/handoffs/` — Session handoff documents generated by `/handoff`. When starting a new session on existing work, read the latest handoff first.